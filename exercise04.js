var firstNum = +prompt("Nhập vào số thứ nhất :");
var secondNum = +prompt("Nhập vào số thứ 2 :");
function addNumbers(firstNum,secondNum) {
     return firstNum + secondNum;
}
alert(addNumbers(firstNum,secondNum));
